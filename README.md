# SQL Course

This repository contains my learnings of SQL and PostgreSQL

## Setup

Use `docker-compose` to setup a postgres database and pg admin.
In the root of this repository use the command below

```bash
docker-compose up -d
```
